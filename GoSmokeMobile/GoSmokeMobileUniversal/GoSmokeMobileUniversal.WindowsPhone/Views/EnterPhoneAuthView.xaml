<views:MvxWindowsPage
    x:Class="GoSmokeMobileUniversal.Views.EnterPhoneAuthView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GoSmokeMobileUniversal.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:Cirrious.MvvmCross.WindowsCommon.Views"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
<StackPanel HorizontalAlignment="Stretch" VerticalAlignment="Center">
           <TextBlock Text="Введите телефон:"/>
            <TextBox Text="{Binding Phone,Mode=TwoWay}"/>
            <Button Content="Далее" Command="{Binding TryLoginCommand}"/>
            <Button Content="Регистрация через ВК" Command="{Binding VkRegisterCommand}"/>
</StackPanel>
        <WebView NavigationStarting="Vk_OnNavigationStarting" Visibility="{Binding BrowserVisibility,Mode=TwoWay}" Source="{Binding BrowserUri,Mode=TwoWay}"   x:Name="Vk">
            
        </WebView>
    </Grid>
</views:MvxWindowsPage>
